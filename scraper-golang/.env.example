# Скопируйте в .env рядом с бинарником или в scraper-golang/.env
# Уже заданные в системе переменные не перезаписываются.

# --- Почта: dev = все письма на TEST_MAIL; prod = на вымышленный seller@gmail.com + проверка RCPT ---
# dev | prod (при dev и -scrape -db таблица listings в SQLite очищается при старте)
ENVIRONMENT=dev
# Куда слать в режиме dev (и при проверке почты в Python)
TEST_MAIL=сlownessmode@bk.ru

# Уведомление в Telegram при блокировке почты; ADMIN_CHAT_ID — чат админа (Go adminbot: cmd/adminbot; clientbot шлёт заявки в этот чат)
# Клиент-бот воркеров: go run ./cmd/clientbot -db ./path/bot.db (прокси: -proxies как у adminbot)
ADMIN_CHAT_ID=691976114
ADMIN_BOT_TOKEN=8646365796:AAEVAb1Nggql6KpyYUsqO3o0F0INmch97OM
CLIENT_BOT_TOKEN=8775734176:AAGGv3zMFK6JndJdMkq4Z91lNl7zXOhASQI

# MP_TEST_MAIL=
